spring:
  profiles:
    active: mysql

---
spring:
  config:
    active:
      on-profile: local
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      username: sa
      password:
      driver-class-name: org.h2.Driver
  batch:
    jdbc:
      initialize-schema: embedded

---
spring:
  config:
    activate:
      on-profile: mysql
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:3306/springbatch?useUnicode=true&characterEncoding=utf8
      username: root
      password: 980605
      driver-class-name: com.mysql.jdbc.Driver
  batch:
    jdbc:
      initialize-schema: always
      # table-prefix: SYSTEM_
    job:
      enabled: true
      # names: batchJob1 # 하드코딩 방식
      names: ${job.name:NONE} # --job.name=batchJob1 (만약 아무 것도 전달 안되면 NONE이라는 이름으로 수행)
